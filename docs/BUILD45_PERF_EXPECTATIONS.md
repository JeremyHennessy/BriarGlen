# Build 45 performance acceptance

The candidate is acceptable only if generated-art snapshot calls stay out of the render hot path, diagnostic draw-site count stays bounded while NPCs move, phone-landscape and phone-portrait render cadence clear the catastrophic-regression floor, and all existing gameplay/art/UI/layout regression suites remain green.

name: UI shell prototype proof

on:
  pull_request:
    paths:
      - 'tests/ui-shell-preview.mjs'
      - 'tests/satchel-preview.mjs'
      - 'tests/map-journal-preview.mjs'
      - '.github/workflows/ui-shell-prototype.yml'
  workflow_dispatch:

jobs:
  ui-shell-preview:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Install Chromium test runtime
        run: |
          npm install --no-save playwright@1.55.0
          npx playwright install chromium
      - name: Start local game server
        run: |
          python3 -m http.server 4173 > /tmp/briarglen-ui-preview.log 2>&1 &
          sleep 2
      - name: Capture reversible UI shell prototype
        run: node tests/ui-shell-preview.mjs http://127.0.0.1:4173/
      - name: Capture reversible Satchel/character prototype
        run: node tests/satchel-preview.mjs http://127.0.0.1:4173/
      - name: Capture reversible Map and Journal prototypes
        run: node tests/map-journal-preview.mjs http://127.0.0.1:4173/
      - name: Upload UI prototype screenshots
        uses: actions/upload-artifact@v4
        with:
          name: ui-shell-prototype-${{ github.sha }}
          path: test-artifacts/ui-shell-preview/*.png
          if-no-files-found: error
          retention-days: 14
